dataset:
  root: Dataset
  train:
    images: train_filtered
    coco_json: train_filtered/_annotations.coco.json
  valid:
    images: valid
    coco_json: valid/_annotations.coco.json
  test:
    images: test
    coco_json: test/_annotations.coco.json

metrics:
  threshold: 0.6
